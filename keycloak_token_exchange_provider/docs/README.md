# Docs

Welcome to the documentation page for this repository! To see the main `README.md`, please check [here](../README.md)!

## How to build jar locally, instead of using remote ghcr

To build jar locally, run the following command:

```sh
make build
```
